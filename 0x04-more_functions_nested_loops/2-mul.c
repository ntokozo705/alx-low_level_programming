#include "main.h"

/**
 * mul - multiplying two integers
 * @a: first integer
 * @b: seconde integer
 * Return: sum of the multiple.
 */

int mul(int a, int b)
{
	int mp;
	
	mp = a *  b;
	return (mp);
}
